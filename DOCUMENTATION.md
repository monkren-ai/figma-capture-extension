# Web to Figma 产品文档

> 版本：1.0.0  
> 更新日期：2026-03-20  
> 作者：派大鑫（小红书）

---

## 目录

1. [产品篇](#一产品篇)
2. [设计篇](#二设计篇)
3. [技术篇](#三技术篇)

---

## 一、产品篇

### 1.1 产品概述

#### 1.1.1 产品定位

**Web to Figma** 是一款 Chrome 浏览器扩展工具，旨在帮助设计师和开发者快速将网页内容转换为 Figma 可编辑的设计稿。通过一键抓取网页元素，自动处理图片资源，生成可直接导入 Figma 的设计文件，大幅提升设计工作流程效率。

#### 1.1.2 目标用户

| 用户群体 | 使用场景 | 核心需求 |
|---------|---------|---------|
| UI/UX 设计师 | 竞品分析、设计参考、快速原型 | 快速获取网页设计元素，用于学习和参考 |
| 前端开发者 | 组件库建设、样式参考 | 提取网页组件，分析设计实现 |
| 产品经理 | 竞品调研、需求文档 | 获取网页截图和设计素材 |
| 运营人员 | 素材收集、内容创作 | 快速获取网页图片和设计资源 |

#### 1.1.3 核心价值

```
┌─────────────────────────────────────────────────────────────┐
│                      核心价值主张                            │
├─────────────────────────────────────────────────────────────┤
│  ✅ 效率提升：从网页到 Figma 设计稿，一键完成，节省 90% 时间   │
│  ✅ 质量保证：自动处理图片资源，保持原始清晰度                 │
│  ✅ 跨域支持：智能代理模式解决跨域图片获取问题                 │
│  ✅ 可控并发：灵活配置图片采集并发数，适应不同网络环境          │
│  ✅ 即开即用：无需注册登录，安装后直接使用                    │
└─────────────────────────────────────────────────────────────┘
```

---

### 1.2 功能说明

#### 1.2.1 功能列表

| 功能模块 | 功能名称 | 功能描述 |
|---------|---------|---------|
| 核心功能 | 网页抓取 | 一键抓取当前网页的所有视觉元素 |
| 核心功能 | Figma 导出 | 生成可直接导入 Figma 的设计文件 |
| 图片处理 | 跨域图片代理 | 解决跨域图片资源获取限制 |
| 图片处理 | 并发控制 | 自定义图片采集并发数量 |
| 图片处理 | 自动等待加载 | 智能等待图片资源加载完成 |
| 用户体验 | 页面内工具栏 | 在网页内显示操作状态和进度 |
| 用户体验 | 实时状态反馈 | 显示抓取进度和结果状态 |

#### 1.2.2 功能详细说明

##### 1.2.2.1 网页抓取

**功能描述**：自动识别并抓取当前网页的所有视觉元素，包括文字、图片、布局结构等。

**技术实现**：
- 解析网页 DOM 结构
- 提取元素样式信息
- 计算元素位置和尺寸
- 处理响应式布局

**使用条件**：
- 用户需在目标网页上激活扩展
- 页面需完成基本加载

##### 1.2.2.2 跨域图片代理模式

**功能描述**：解决浏览器跨域安全策略导致的图片资源获取限制。

**工作原理**：
```
┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│   目标网页    │ ───▶ │   代理服务    │ ───▶ │   本地存储    │
│  (跨域图片)   │      │  (中转请求)   │      │  (图片资源)   │
└──────────────┘      └──────────────┘      └──────────────┘
```

**配置选项**：
- 开启：启用代理模式，可获取跨域图片
- 关闭：直接请求图片资源，速度更快但可能受限

**适用场景**：
- 开启：目标网站图片有跨域限制
- 关闭：目标网站图片无跨域限制或同源

##### 1.2.2.3 图片采集并发控制

**功能描述**：控制同时下载图片的数量，平衡速度和稳定性。

**并发选项**：

| 并发数 | 适用场景 | 特点 |
|-------|---------|------|
| 4 | 网络较慢、服务器限制严格 | 最稳定，速度较慢 |
| 6 | 一般网络环境 | 平衡稳定性和速度 |
| 8（默认） | 推荐设置 | 最佳平衡点 |
| 10 | 网络良好 | 速度较快 |
| 12 | 高速网络 | 快速采集 |
| 16 | 企业级网络 | 高速采集 |
| 20 | 极速网络 | 最快速度 |
| 无限 | 不限制并发数 | 最快但可能触发服务器限制 |

---

### 1.3 用户场景

#### 1.3.1 典型使用流程

```
┌─────────────────────────────────────────────────────────────────────┐
│                         用户使用流程                                 │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  Step 1: 打开目标网页                                                │
│     └── 用户在 Chrome 浏览器中打开需要抓取的网页                       │
│                                                                     │
│  Step 2: 点击扩展图标                                                │
│     └── 点击浏览器工具栏中的 Web to Figma 扩展图标                    │
│                                                                     │
│  Step 3: 配置参数（可选）                                            │
│     ├── 开启/关闭跨域图片代理模式                                     │
│     └── 选择图片采集并发数                                           │
│                                                                     │
│  Step 4: 开始采集                                                    │
│     └── 点击"开始采集"按钮                                           │
│                                                                     │
│  Step 5: 等待完成                                                    │
│     └── 扩展自动抓取页面元素并处理图片资源                             │
│                                                                     │
│  Step 6: 获取结果                                                    │
│     └── 自动下载 Figma 可编辑的设计文件                               │
│                                                                     │
│  Step 7: 导入 Figma                                                  │
│     └── 在 Figma 中导入下载的文件进行编辑                             │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

#### 1.3.2 典型场景案例

##### 场景一：竞品设计分析

**用户角色**：UI 设计师  
**需求背景**：需要分析竞品网站的设计风格和布局结构  
**操作步骤**：
1. 打开竞品网站页面
2. 点击 Web to Figma 扩展
3. 使用默认配置，点击"开始采集"
4. 获取设计文件后，在 Figma 中分析设计细节

**价值收益**：将数小时的手动截图和测量工作缩短至几分钟

##### 场景二：设计灵感收集

**用户角色**：视觉设计师  
**需求背景**：收集优秀网站的设计元素作为灵感参考  
**操作步骤**：
1. 浏览设计灵感网站（如 Dribbble、Behance 等）
2. 发现喜欢的页面设计
3. 使用扩展快速抓取
4. 在 Figma 中建立灵感库

**价值收益**：快速建立可编辑的设计灵感素材库

##### 场景三：组件库建设

**用户角色**：前端开发者  
**需求背景**：参考优秀网站的组件设计，建设内部组件库  
**操作步骤**：
1. 打开目标网站
2. 使用扩展抓取页面
3. 在 Figma 中提取需要的组件
4. 转换为内部组件库的设计规范

**价值收益**：加速组件库设计阶段，保持设计一致性

---

### 1.4 产品限制

| 限制类型 | 具体说明 | 解决方案 |
|---------|---------|---------|
| 浏览器限制 | 仅支持 Chrome 浏览器 | 使用 Chrome 或 Chromium 内核浏览器 |
| 动态内容 | 无法抓取动态加载的内容 | 等待页面完全加载后再采集 |
| 登录页面 | 无法抓取需要登录的页面 | 先登录后再采集 |
| Canvas/SVG | 复杂 Canvas 绘制可能无法完美还原 | 手动补充调整 |
| 视频元素 | 视频元素以静态帧形式呈现 | 后续手动处理 |

---

## 二、设计篇

### 2.1 UI 设计规范

#### 2.1.1 设计原则

```
┌─────────────────────────────────────────────────────────────┐
│                        设计原则                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  🎯 简洁高效                                                 │
│     └── 最小化操作步骤，一键完成任务                           │
│                                                             │
│  🎯 信息清晰                                                 │
│     └── 状态反馈明确，用户始终了解当前进度                      │
│                                                             │
│  🎯 配置灵活                                                 │
│     └── 提供可选配置，满足不同场景需求                          │
│                                                             │
│  🎯 视觉一致                                                 │
│     └── 遵循系统设计语言，保持原生体验                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 2.1.2 色彩规范

| 用途 | 色值 | 使用场景 |
|------|------|---------|
| 主背景色 | `#f7f8fa` | 弹窗背景 |
| 主按钮背景 | `#000000` | 主要操作按钮 |
| 主按钮文字 | `#ffffff` | 按钮文字 |
| 标题文字 | `#000000` | 标题文本 |
| 正文文字 | `#333333` | 正文内容 |
| 辅助文字 | `#666666` | 提示说明 |
| 分割线 | `#e5e5e5` | 分隔区域 |

#### 2.1.3 字体规范

```css
font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
```

| 元素 | 字号 | 字重 | 行高 |
|------|------|------|------|
| 标题 | 16px | 600 | 24px |
| 正文 | 14px | 400 | 20px |
| 辅助文字 | 12px | 400 | 18px |
| 按钮文字 | 14px | 500 | 20px |

#### 2.1.4 间距规范

| 类型 | 数值 | 使用场景 |
|------|------|---------|
| 容器内边距 | 16px | 弹窗内容区域 |
| 元素间距 | 12px | 各功能模块之间 |
| 紧凑间距 | 8px | 标签与内容之间 |
| 按钮内边距 | 12px 24px | 按钮文字与边框 |

#### 2.1.5 圆角规范

| 元素 | 圆角值 |
|------|--------|
| 按钮 | 8px |
| 输入框 | 6px |
| 卡片容器 | 8px |
| 开关控件 | 12px（半圆） |

---

### 2.2 界面设计

#### 2.2.1 Popup 弹窗设计

```
┌────────────────────────────────────────┐
│                                        │
│           Web to Figma                 │
│                                        │
├────────────────────────────────────────┤
│                                        │
│  跨域图片代理模式          [开关控件]   │
│                                        │
├────────────────────────────────────────┤
│                                        │
│  图片采集并发数                         │
│  [ 4 ] [ 6 ] [ 8 ] [ 10 ] [ 12 ] ...   │
│                                        │
├────────────────────────────────────────┤
│                                        │
│  💡 提示：开启代理模式可获取跨域图片     │
│                                        │
├────────────────────────────────────────┤
│                                        │
│         [  开始采集  ]                  │
│                                        │
├────────────────────────────────────────┤
│                                        │
│  状态：准备就绪                         │
│                                        │
└────────────────────────────────────────┘
```

**设计说明**：

| 区域 | 设计要点 |
|------|---------|
| 标题区 | 居中显示产品名称，简洁醒目 |
| 配置区 | 开关控件和选项按钮，操作直观 |
| 提示区 | 浅色背景区分，提供操作指引 |
| 操作区 | 主按钮居中，视觉突出 |
| 状态区 | 实时反馈当前状态 |

#### 2.2.2 页面内工具栏设计

```
┌─────────────────────────────────────────────────────────┐
│  [图标]  Web to Figma  │  采集中... 50%  │  [取消]      │
└─────────────────────────────────────────────────────────┘
```

**设计要点**：
- 固定在页面底部或顶部
- 半透明背景，不遮挡主要内容
- 实时显示进度百分比
- 提供取消操作入口

---

### 2.3 交互设计

#### 2.3.1 交互流程图

```
                    ┌─────────────┐
                    │   初始状态   │
                    └──────┬──────┘
                           │
                    用户点击扩展图标
                           │
                           ▼
                    ┌─────────────┐
                    │   弹窗打开   │
                    │  显示配置项  │
                    └──────┬──────┘
                           │
              ┌────────────┼────────────┐
              │            │            │
        配置代理模式   选择并发数    直接开始
              │            │            │
              └────────────┼────────────┘
                           │
                    点击"开始采集"
                           │
                           ▼
                    ┌─────────────┐
                    │   采集进行中 │
                    │  显示进度条  │
                    └──────┬──────┘
                           │
              ┌────────────┼────────────┐
              │            │            │
           采集成功     采集失败     用户取消
              │            │            │
              ▼            ▼            ▼
        ┌──────────┐ ┌──────────┐ ┌──────────┐
        │ 下载文件 │ │ 显示错误 │ │ 返回初始 │
        │ 显示成功 │ │ 提供重试 │ │   状态   │
        └──────────┘ └──────────┘ └──────────┘
```

#### 2.3.2 状态设计

| 状态 | 显示文案 | 视觉表现 |
|------|---------|---------|
| 初始 | 准备就绪 | 灰色文字 |
| 采集中 | 采集进行中... X% | 蓝色进度条 |
| 成功 | 采集完成！ | 绿色成功图标 |
| 失败 | 采集失败：[错误原因] | 红色错误提示 |

#### 2.3.3 反馈设计

**即时反馈**：
- 按钮点击：视觉按下效果
- 开关切换：动画过渡
- 选项选择：高亮选中状态

**进度反馈**：
- 百分比数字显示
- 进度条动画
- 当前处理步骤提示

**结果反馈**：
- 成功：自动下载 + 成功提示
- 失败：错误原因 + 重试选项

---

### 2.4 响应式设计

#### 2.4.1 弹窗尺寸

| 属性 | 数值 |
|------|------|
| 最小宽度 | 300px |
| 最大宽度 | 400px |
| 内边距 | 16px |

#### 2.4.2 适配策略

- 弹窗宽度自适应内容
- 文字换行处理
- 按钮宽度自适应

---

## 三、技术篇

### 3.1 技术架构

#### 3.1.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                         Chrome Extension                             │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐             │
│  │   Popup     │    │ Background  │    │  Content    │             │
│  │   (UI层)    │◄──▶│   Service   │◄──▶│   Script    │             │
│  │             │    │   Worker    │    │             │             │
│  └─────────────┘    └─────────────┘    └─────────────┘             │
│         │                  │                  │                     │
│         │                  │                  │                     │
│         ▼                  ▼                  ▼                     │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐             │
│  │  popup.js   │    │background.js│    │ capture.js  │             │
│  │  popup.css  │    │             │    │ runner.js   │             │
│  │  popup.html │    │             │    │inpage-      │             │
│  │             │    │             │    │ toolbar.js  │             │
│  └─────────────┘    └─────────────┘    └─────────────┘             │
│                                                                     │
├─────────────────────────────────────────────────────────────────────┤
│                          Chrome APIs                                │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐   │
│  │ activeTab   │ │  scripting  │ │  downloads  │ │   storage   │   │
│  └─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

#### 3.1.2 模块职责

| 模块 | 文件 | 职责描述 |
|------|------|---------|
| Popup UI | popup.html/css/js | 用户界面，配置入口，状态显示 |
| Background Service | background.js | 消息路由，脚本注入，图片代理，文件下载 |
| Capture Engine | capture.js | DOM 解析，元素提取，样式计算 |
| Runner | runner.js | 执行流程控制，任务调度 |
| In-page Toolbar | inpage-toolbar.js | 页面内嵌工具栏，状态展示 |

#### 3.1.3 通信架构

```
┌───────────────┐                      ┌───────────────┐
│   Popup UI    │                      │   Background  │
│               │                      │   Service     │
│  popup.js     │◄───── Message ──────▶│               │
│               │                      │  background.js│
└───────────────┘                      └───────┬───────┘
                                               │
                                               │ Script Injection
                                               │
                                               ▼
                                       ┌───────────────┐
                                       │    Content    │
                                       │    Script     │
                                       │               │
                                       │  capture.js   │
                                       │  runner.js    │
                                       └───────────────┘
```

---

### 3.2 核心技术实现

#### 3.2.1 Manifest V3 配置

```json
{
  "manifest_version": 3,
  "name": "Web to Figma",
  "version": "1.0.0",
  "description": "一键抓取网页内容并转换为 Figma 可编辑的设计稿",
  "permissions": [
    "activeTab",
    "scripting",
    "downloads",
    "storage"
  ],
  "host_permissions": [
    "<all_urls>"
  ],
  "action": {
    "default_popup": "popup.html",
    "default_icon": {
      "16": "logo/icon16.png",
      "48": "logo/icon48.png",
      "128": "logo/icon128.png"
    }
  },
  "background": {
    "service_worker": "background.js"
  },
  "web_accessible_resources": [
    {
      "resources": [
        "capture.js",
        "runner.js",
        "logo/icon16.png"
      ],
      "matches": ["<all_urls>"]
    }
  ]
}
```

#### 3.2.2 权限说明

| 权限 | 用途 | 必要性 |
|------|------|--------|
| `activeTab` | 访问当前活动标签页 | 必须 - 获取页面内容 |
| `scripting` | 注入脚本到页面 | 必须 - 执行抓取逻辑 |
| `downloads` | 下载生成的文件 | 必须 - 导出结果文件 |
| `storage` | 存储用户配置 | 可选 - 记住用户设置 |
| `<all_urls>` | 访问所有网站 | 必须 - 抓取任意网页 |

#### 3.2.3 消息通信协议

**Popup → Background**

```javascript
{
  action: 'startCapture',
  config: {
    proxyMode: boolean,
    concurrency: number
  }
}
```

**Background → Popup**

```javascript
{
  status: 'progress' | 'success' | 'error',
  progress: number,
  message: string,
  data?: any
}
```

#### 3.2.4 图片处理流程

```
┌─────────────────┐
│   发现图片元素   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   判断图片类型   │
└────────┬────────┘
         │
    ┌────┴────┐
    │         │
    ▼         ▼
┌───────┐ ┌───────┐
│ 同源  │ │ 跨域  │
└───┬───┘ └───┬───┘
    │         │
    │    ┌────┴────┐
    │    │         │
    │    ▼         ▼
    │ ┌───────┐ ┌───────┐
    │ │代理开启│ │代理关闭│
    │ └───┬───┘ └───┬───┘
    │     │         │
    │     ▼         ▼
    │  代理请求   尝试直接请求
    │     │         │
    └─────┴─────────┘
              │
              ▼
      ┌───────────────┐
      │  并发控制下载  │
      └───────┬───────┘
              │
              ▼
      ┌───────────────┐
      │  转换为Base64  │
      └───────┬───────┘
              │
              ▼
      ┌───────────────┐
      │  嵌入设计文件  │
      └───────────────┘
```

---

### 3.3 数据存储

#### 3.3.1 存储结构

```javascript
chrome.storage.local.get(['config'], (result) => {
  const config = result.config || {
    proxyMode: false,
    concurrency: 8
  };
});
```

#### 3.3.2 配置持久化

| 配置项 | 默认值 | 说明 |
|-------|-------|------|
| proxyMode | false | 跨域图片代理模式 |
| concurrency | 8 | 图片采集并发数 |

---

### 3.4 部署指南

#### 3.4.1 开发环境安装

```bash
# 1. 克隆或下载项目
cd /path/to/figma-capture-extension

# 2. 打开 Chrome 扩展管理页面
# chrome://extensions/

# 3. 开启开发者模式

# 4. 点击"加载已解压的扩展程序"

# 5. 选择项目目录
```

#### 3.4.2 生产环境发布

**发布前检查清单**：

- [ ] 更新 manifest.json 中的版本号
- [ ] 确保所有代码已混淆压缩
- [ ] 准备扩展图标（16x16、48x48、128x128）
- [ ] 准备宣传图片（440x280、920x680、1400x560）
- [ ] 编写商店描述和截图

**发布步骤**：

```
1. 登录 Chrome Web Store 开发者控制台
   https://chrome.google.com/webstore/developer/dashboard

2. 点击"添加新项目"

3. 上传扩展 ZIP 包
   - 包含所有必要文件
   - 不包含源码映射文件

4. 填写商店信息
   - 名称：Web to Figma
   - 简介：一键抓取网页内容并转换为 Figma 设计稿
   - 详细描述：功能说明、使用方法
   - 分类：生产力工具
   - 语言：中文（简体）

5. 上传宣传素材
   - 截图：展示主要功能
   - 宣传图：突出核心价值

6. 设置隐私政策
   - 说明数据收集和使用情况

7. 提交审核
```

#### 3.4.3 版本更新流程

```
┌─────────────────┐
│  更新代码/功能   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 更新版本号      │
│ manifest.json   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  代码混淆压缩   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  本地测试验证   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  打包上传更新   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  等待审核通过   │
└─────────────────┘
```

---

### 3.5 性能优化

#### 3.5.1 优化策略

| 优化项 | 实现方式 | 效果 |
|-------|---------|------|
| 代码混淆 | 变量名替换、字符串编码 | 减小文件体积，保护代码 |
| 并发控制 | 队列管理、限流机制 | 避免服务器压力过大 |
| 懒加载 | 按需注入脚本 | 减少内存占用 |
| 缓存策略 | 存储用户配置 | 提升用户体验 |

#### 3.5.2 性能指标

| 指标 | 目标值 |
|------|-------|
| 弹窗打开时间 | < 100ms |
| 脚本注入时间 | < 50ms |
| 单页抓取时间 | < 10s（取决于图片数量） |
| 内存占用 | < 100MB |

---

### 3.6 安全考虑

#### 3.6.1 安全措施

| 安全项 | 实现方式 |
|-------|---------|
| 内容安全策略 | 限制脚本执行来源 |
| 敏感数据保护 | 不存储用户隐私信息 |
| 权限最小化 | 仅申请必要权限 |
| 跨域处理 | 使用代理模式安全获取资源 |

#### 3.6.2 隐私声明

```
本扩展承诺：
1. 不收集任何用户个人信息
2. 不上传用户数据到第三方服务器
3. 所有处理均在本地完成
4. 用户配置仅存储在本地浏览器
```

---

### 3.7 错误处理

#### 3.7.1 错误类型

| 错误类型 | 错误原因 | 处理方式 |
|---------|---------|---------|
| 页面加载失败 | 目标页面未完全加载 | 提示用户等待后重试 |
| 脚本注入失败 | 页面安全策略限制 | 提示用户页面不支持 |
| 图片下载失败 | 网络错误或权限限制 | 跳过该图片，继续处理 |
| 文件生成失败 | 内存不足或格式错误 | 提示错误信息，提供重试 |

#### 3.7.2 错误恢复机制

```javascript
try {
  await capturePage();
} catch (error) {
  if (error.type === 'NETWORK_ERROR') {
    showRetryOption();
  } else if (error.type === 'PERMISSION_ERROR') {
    showPermissionGuide();
  } else {
    showErrorDetails(error.message);
  }
}
```

---

### 3.8 未来规划

#### 3.8.1 功能规划

| 版本 | 计划功能 | 优先级 |
|------|---------|--------|
| v1.1 | 支持选择区域抓取 | 高 |
| v1.2 | 支持批量页面抓取 | 中 |
| v1.3 | 支持自定义导出格式 | 中 |
| v1.4 | 支持快捷键操作 | 低 |
| v2.0 | 支持 Firefox 浏览器 | 低 |

#### 3.8.2 技术优化

- [ ] 引入 TypeScript 提升代码质量
- [ ] 添加单元测试覆盖
- [ ] 优化大页面抓取性能
- [ ] 支持更多图片格式

---

## 附录

### A. 常见问题 FAQ

**Q1: 为什么有些图片无法抓取？**  
A: 可能是跨域限制导致，请尝试开启"跨域图片代理模式"。

**Q2: 抓取后的设计稿在 Figma 中显示不完整？**  
A: 部分复杂样式（如特殊滤镜、动画）可能无法完美还原，需要手动调整。

**Q3: 支持哪些浏览器？**  
A: 目前仅支持 Chrome 浏览器（包括 Chromium 内核浏览器如 Edge）。

**Q4: 是否需要登录或注册？**  
A: 不需要，安装后即可直接使用。

---

### B. 更新日志

| 版本 | 日期 | 更新内容 |
|------|------|---------|
| 1.0.0 | 2026-03-20 | 初始版本发布 |

---

### C. 联系方式

- **作者**：派大鑫
- **平台**：小红书
- **反馈渠道**：Chrome Web Store 评论

---

*文档结束*
